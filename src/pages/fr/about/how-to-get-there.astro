---
import SectionLayout from '../../../layouts/SectionLayout.astro';
import GoogleMap from '../../../components/GoogleMap.astro';
import { getEntry, render } from 'astro:content';

const entry = await getEntry('pages', 'fr/how-to-get-there');
const enEntry = await getEntry('pages', 'en/how-to-get-there');
if (!entry) {
  throw new Error('How to get there page content not found (FR)');
}
const { Content } = await render(entry);
---

<SectionLayout 
  title={entry.data.title} 
  section="about" 
  activePage="howto" 
  description="Comment venir à La Loingtaine — accès par train et voiture."
  heroImage={entry.data.image || enEntry?.data.image || "/images/site/home-hero.jpg"}
>
  <div class="section-intro">
    <h2>{entry.data.title}</h2>
    <div class="content-body">
      <Content />
    </div>
    <div class="map-wrapper">
      <GoogleMap
        address="40 rue Grande, Sorques, 77690 Montigny sur Loing, France"
        zoom={15}
        height="350px"
      />
    </div>
  </div>
</SectionLayout>


